# Copy to .env in your Codespace or set as Codespaces environment variable.
# A strong secret token required to call /api/config. This MUST be set.
ADMIN_TOKEN=changeme-REPLACE_THIS
# Server port (Codespaces normally exposes port 8080 by default)
PORT=8080
